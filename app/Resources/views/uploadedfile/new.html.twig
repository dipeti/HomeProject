{% extends 'base.html.twig' %}

{% block pagecontent %}
<div class="container">
    <h1>Uploadedfile creation</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <hr>
    <a class="btn btn-default" href="{{ path('downloads_index') }}">Back to the list</a>
</div>
{% endblock %}
